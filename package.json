{
  "name": "blade-provider",
  "module": "src/api/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build:native": "bun run scripts/build-native.ts",
    "dev": "bun run build:native && bun --hot src/api/index.ts",
    "start": "bun run build:native && bun run src/api/index.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
