/**
 * internal.h
 *
 * Private shared definitions for the BCD algorithm module.
 * Contains internal structs, types, constants, and helper function
 * signatures used across bcd_metadata.c and compute/ files.
 * Must not be included outside of boustrophedon_cellular_decomposition/.
 *
 * Included by: bcd_metadata.c, bcd_compute.c, step files
 */

